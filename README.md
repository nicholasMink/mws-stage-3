# Restaurant Reviews  
MWS - Stage 3 Project

### Running the application
#### First, start the local development API server
  ``` bash
  $ cd server/
  $ npm install
  $ node app.js
  ```

#### From a second terminal, run:
  ``` bash
  $ cd public/
  $ npm install
  $ gulp
  ```